Coloca aqui tu esquema SQL inicial o un respaldo compatible con PostgreSQL.

Los archivos `.sql` o `.sql.gz` dentro de esta carpeta se ejecutan automaticamente
la primera vez que se crea el contenedor de `db`.

Ejemplo:
- `001-schema.sql`
- `002-seed.sql`
